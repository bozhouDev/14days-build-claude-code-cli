# Python Package Template

This is a minimal Python package template for the 7-day `agent-code` tutorial.

It is not a real day implementation. Use it as the starting shape for future Python day snapshots.

## Run

```bash
uv run agent-code
```

Expected output:

```txt
hello from template
```

## Test

```bash
uv run --extra test pytest
```
